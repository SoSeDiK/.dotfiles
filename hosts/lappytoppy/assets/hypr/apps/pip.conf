# Picture-in-picture overlays
windowrule = tag +pip, match:title ^(Picture.?in.?[Pp]icture)$

windowrule {
    name = PiP
    match:tag = pip

    float = on
    pin = on
    size = 600 338
    keep_aspect_ratio = on
    border_size = 0
    opacity = 1 1
    move = 100%-w-40 4%
}
