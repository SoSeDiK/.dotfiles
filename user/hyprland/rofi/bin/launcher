#!/usr/bin/env bash

# Import Current Theme
DIR="$HOME/.dotfiles/user/hyprland/rofi"
THEME="simple"
RASI="$DIR/themes/$THEME/launcher.rasi"

rofi \
    -show drun -no-levenshtein-sort -hover-select -me-select-entry '' -me-accept-entry MousePrimary \
	-theme ${RASI}
