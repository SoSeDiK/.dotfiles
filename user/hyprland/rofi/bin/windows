#!/usr/bin/env bash

# Import Current Theme
DIR="$HOME/.dotfiles/user/hyprland/rofi"
THEME="simple"
RASI="$DIR/themes/$THEME/windows.rasi"

rofi \
    -show window -no-levenshtein-sort -hover-select -me-select-entry '' -me-accept-entry MousePrimary \
	-theme ${RASI}
